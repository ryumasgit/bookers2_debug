<div class="row">
  <div class="col">
    <%= form_with model: [book, book_comment], local: false do |f| %>
      <div class="mb-2">
        <%= f.text_area :comment, rows: '5', class: 'form-control', style: 'border-color: black;'%>
      </div>
        <%= f.submit "送信" %>
    <% end %>
  </div>
</div>